'use client';
import React from 'react'

const Experience = () => {
  return (
    <div>
        <button onClick={() => console.log('Click')}>Click</button>
    </div>
  )
}

export default Experience